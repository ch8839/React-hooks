import React, { useState, useEffect, useMemo, useCallback, useRef } from 'react'

interface Props {
  CSSProperty: React.CSSProperties['whiteSpace'] | null
}

export const Promise = (props: Props) => {
  
  return (
    <div>
     
    </div>
  )
}
